$("#inner-messages").prepend("<%= j render @messages.reverse %>");

<% if @messages.next_page %>
    $(".pagination").replaceWith("<%= j will_paginate @messages %>");
<% else %>
    $(window).off('scroll');
    $(".pagination").remove();
<% end %>